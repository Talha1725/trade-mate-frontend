name: Prevent lockfiles

on:
  push:
  pull_request:

jobs:
  block-lockfiles:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fail on lockfiles
        run: node scripts/check-lockfiles.mjs
